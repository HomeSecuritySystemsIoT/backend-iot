services:
  backend:
    build: .
    restart: always
    ports:
      - "7890:7890"
      - "7891:7891"
      - "7892:7892/udp"
    volumes:
      - ./images_received:/app/images_received
      - ./logs:/app/logs
